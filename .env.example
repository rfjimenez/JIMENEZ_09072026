#Copy this file up to .env and replace the value below before running
#docker compose up. The .env file is git-ignored so the key is never committed.
#
#The application deliberately has no default API key: it fails fast at startup
#if one is not supplied, rather than starting with authentication that cannot
#succeed.

#Any non-empty string.
ApiKey__Value=api-key-for-demo